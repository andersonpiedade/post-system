<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: #3f8f56;
        }

        header {
            display: flex;
            align-items: center;
            padding: 15px;
            justify-content: space-around;
            font-family: sans-serif;
            background: rgb(31, 31, 31);
            color: #fff;
            height: 70px;
        }

        .description {
            text-align: center;
            margin-top: 15px;
            color: #fff;
        }

        #menu {
            display: flex;
            list-style: none;
            gap: 2rem;
        }

        #menu a {
            color: #fff;
            text-decoration: none;
        }

        #menu a:hover {
            text-decoration: underline;
        }

        form {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background: #000;
            padding: 30px;
            color: #fff;
            width: 700px;
            height: 400px;
            overflow-y: auto;
            margin-top: 40px;
            border-radius: 15px;
        }

        .legend-title {
           font-size: 20px;
        }

        .title {
            width: 100%;
            height: 50px;
            text-align: center;
            outline: none;
            font-size: 25px;
        }

        .legend-content {
            font-size: 20px;
        }

        .content {
            width: 100%;
            outline: none;
            font-size: 18px;
            padding: 10px;
            text-transform: justfy;
        }

        .botao {
            width: 100%;
            padding: 15px;
            border: none;
            font-family: sans-serif;
            font-size: 18px;
            cursor: pointer;
            background: rgb(31, 31, 31);
            color: #fff;
        }


    </style>
</head>
<body>
    <header>

        <div class="logo">
            <h2>Logo</h2>
        </div><!-- logo -->
        <nav>
            <ul id="menu">
                <li><a href="/">Home</a></li>
                <li><a href="./form">Postar Conteúdo</a></li>
                <li><a href="./posts">Ver Posts</a></li>
            </ul>
        </nav>

    </header>

    <h1 class="description">Post o que quiser</h1>


    <form action="/add" method="post">
        <p class="legend-title">Título</p>
        <br>
        <input type="text" name="titulo" class="title" placeholder="Adiciona um título a sua postagem">
        <br>
        <br>
        <p class="legend-content">Conteúdo</p>
        <br>
        <textarea name="conteudo" cols="88" rows="15" class="content" placeholder="Digite um texto para postar"></textarea>
        <br>
        <br>
        <button type="submit" class="botao" title="Postar">Cadastrar postagem</button>
    </form>

</body>
</html>